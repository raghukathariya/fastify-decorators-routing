{
  "name": "fastify-decorators-routing-example",
  "private": true,
  "version": "0.0.0",
  "description": "A runnable example app demonstrating fastify-decorators-routing end to end.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/main.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "fastify-decorators-routing": "file:..",
    "@fastify/swagger": "^9.8.1",
    "@fastify/swagger-ui": "^5.2.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "fastify": "^5.2.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
